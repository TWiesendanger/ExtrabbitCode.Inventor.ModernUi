<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}" />
        <Setter Property="FontFamily" Value="{DynamicResource Font.Family}" />
        <Setter Property="FontSize" Value="{DynamicResource Font.Size.Normal}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Grid Width="18" Height="18" VerticalAlignment="Center">
                            <!-- Outward grey glow on hover (same technique as the button): the opaque
                                 Box on top covers the inner half, so only the outer halo shows. -->
                            <Border x:Name="Glow"
                                    CornerRadius="3"
                                    Background="{DynamicResource Brush.Control}"
                                    Opacity="0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#C8C8C8" ShadowDepth="0" BlurRadius="6" Opacity="0.9" />
                                </Border.Effect>
                            </Border>

                            <Border x:Name="Box"
                                    Background="{DynamicResource Brush.Control}"
                                    BorderBrush="{DynamicResource Brush.Foreground}"
                                    BorderThickness="0.6"
                                    CornerRadius="3"
                                    UseLayoutRounding="False"
                                    SnapsToDevicePixels="False">
                                <Path x:Name="Check"
                                      Width="11" Height="11"
                                      Stretch="Uniform"
                                      Data="M 0,5 L 4,9 L 11,0"
                                      Stroke="{DynamicResource Brush.Background}"
                                      StrokeThickness="2"
                                      StrokeEndLineCap="Round"
                                      StrokeStartLineCap="Round"
                                      Visibility="Collapsed" />
                            </Border>
                        </Grid>
                        <ContentPresenter Margin="8,0,0,0"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <!-- White box (foreground) with a dark check; visible and theme-aware. -->
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource Brush.Foreground}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Foreground}" />
                            <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource Brush.AccentMuted}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Accent}" />
                        </Trigger>
                        <!-- Hover: outward grey glow only (matches the button). -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Glow" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
