<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Header click target: a panel-colored bar with a rotating triangle + the Header content.
         A standalone ToggleButton template so the Expander template below stays readable. -->
    <ControlTemplate x:Key="ModernExpanderToggle" TargetType="ToggleButton">
        <Border x:Name="Header"
                Background="{DynamicResource Brush.Panel}"
                CornerRadius="{DynamicResource Radius.Control}"
                Padding="8,6"
                SnapsToDevicePixels="True">
            <StackPanel Orientation="Horizontal">
                <!-- Right-pointing triangle (collapsed). Rotates to point down when expanded. -->
                <Path x:Name="Arrow"
                      Width="8" Height="9"
                      Margin="2,0,8,0"
                      VerticalAlignment="Center"
                      Data="M 0,0 L 5,4.5 L 0,9 Z"
                      Fill="{DynamicResource Brush.Foreground}"
                      RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <RotateTransform Angle="0" />
                    </Path.RenderTransform>
                </Path>
                <ContentPresenter VerticalAlignment="Center"
                                  RecognizesAccessKey="True"
                                  SnapsToDevicePixels="True" />
            </StackPanel>
        </Border>
        <ControlTemplate.Triggers>
            <!-- Expanded: square off the bottom corners (content joins below) and turn the arrow. -->
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Header" Property="CornerRadius" Value="4,4,0,0" />
                <Setter TargetName="Arrow" Property="RenderTransform">
                    <Setter.Value>
                        <RotateTransform Angle="90" />
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource Brush.Accent}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="Expander">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}" />
        <Setter Property="FontFamily" Value="{DynamicResource Font.Family}" />
        <Setter Property="FontSize" Value="{DynamicResource Font.Size.Normal}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <DockPanel>
                        <ToggleButton x:Name="HeaderToggle"
                                      DockPanel.Dock="Top"
                                      OverridesDefaultStyle="True"
                                      Focusable="True"
                                      Template="{StaticResource ModernExpanderToggle}"
                                      Content="{TemplateBinding Header}"
                                      ContentTemplate="{TemplateBinding HeaderTemplate}"
                                      Foreground="{TemplateBinding Foreground}"
                                      FontFamily="{TemplateBinding FontFamily}"
                                      FontSize="{TemplateBinding FontSize}"
                                      IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />

                        <Border x:Name="ContentArea"
                                DockPanel.Dock="Bottom"
                                Visibility="Collapsed"
                                Background="{DynamicResource Brush.Background}"
                                CornerRadius="0,0,4,4"
                                Padding="{DynamicResource Padding.Card}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="True" />
                        </Border>
                    </DockPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ContentArea" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
